[package]
name = "stratrix"
version = "0.1.0"
edition = "2021"

[dependencies]
num-traits = "0.2"
rayon = "1.8"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "matrix_bench"
harness = false
